# How I setup my work MBP

I use bash shell, not zsh.

    sudo chsh -s $(which bash) hendry


# Check keyboard is setup how I like it

	❯ defaults read -g KeyRepeat -currentHost
	2

	~
	❯ defaults read -g InitialKeyRepeat -currentHost
	15
